<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.zed">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Feature for devices with camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" /> <!-- QUERIES goes here: Allows app to find the camera app -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:forceDarkAllowed="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zed">
        <activity
            android:name=".detailed_goods_received_note"
            android:exported="false" />
        <activity
            android:name=".goods_received_note"
            android:exported="false" />
        <activity
            android:name=".purchase_requisition"
            android:exported="false" />
        <activity
            android:name=".CameraActivity"
            android:exported="false" /> <!-- Your Activities -->
        <activity
            android:name=".stockList"
            android:exported="false" />
        <activity
            android:name=".stock_fragment"
            android:exported="false" />
        <activity
            android:name=".signup_dashboard_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InventoryItemDetails"
            android:exported="false" />
        <activity
            android:name=".stockTaking"
            android:exported="false" />
        <activity
            android:name=".PhysicalInventory"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".dashboardActivity"
            android:exported="false" />
        <activity
            android:name=".cashTracker"
            android:exported="false" /> <!-- FILEPROVIDER goes here: Critical for the camera to save photos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>